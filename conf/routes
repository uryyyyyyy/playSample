# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET      /account/signin                              controllers.Application.showSignIn
# サインアウト処理
GET      /account/signout                             controllers.Application.signOut
# GitHubからのコールバックURL(はじめにGitHubで設定したものと同じ)
GET      /callback                         controllers.Application.callBackGitHub(code:String)